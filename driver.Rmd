---
title: "RMarkdown Report Driving `targets` pipeline"
author: "Ian Flores Siaca"
date: "`r Sys.Date()`"
resource_files:
- R/connect_helpers.R
- _targets.R
- renv.lock
---

```{r setup, message=FALSE, warning=FALSE}
library(targets)
library(visNetwork)
library(tidymodels)
library(tidyverse)
library(connectapi)
library(rsconnect)
library(tarchetypes)
library(aws.s3)
library(qs)
```

```{r s3-bucket-setup}
bucket_name <- Sys.getenv("AWS_BUCKET_NAME", "targets-deployment-bucket")

if (bucket_exists(bucket_name)){
  print("Bucket already exists")
} else {
  print("Creating Bucket")
  put_bucket(bucket_name)
}
```

```{r glimpse}
tar_glimpse()
```

```{r plan}
tar_visnetwork()
```

```{r make}
tar_make()
```
